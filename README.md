# Payroll Web (React 18)

Run `npm install`, `npm test`, and `npm run build`. The sample pages demonstrate setup navigation and the golden draft payslip. Generate a typed client from the supplied OpenAPI contract before implementing server integration.
